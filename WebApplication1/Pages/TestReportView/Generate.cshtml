@page
@model GenerateModel
@{
    ViewData["Title"] = "测试报告";
}
<div>
    <a asp-page="./Index">前往测试报告汇总页面</a>
</div>
<h1>设备信息</h1>
<p>设备编号: @Model.DeviceInfo?.DeviceId</p>
<p>设备名: @Model.DeviceInfo?.Name</p>
<p>设备生产商: @Model.DeviceInfo?.Manufacturer</p>

<h2>测试报告编号：@Model.newTestReport?.Id</h2>
<p>测试时间：@Model.newTestReport?.TestTime</p>

@if (Model.newTestReport?.TestResults != null && Model.newTestReport.TestResults.Any())
{
        <table class="table">
            <thead>
                <tr>
                    <th>测试编号</th>
                    <th>二次电压</th>
                    <th>温度</th>
                    <th>湿度</th>
                    <th>测试时间</th>
                    <th>tan</th>
                    <th>R</th>
                </tr>
            </thead>
            <tbody>
            @foreach (var result in Model.newTestReport.TestResults)
            {
                    <tr>
                        <td>@result.TestId</td>
                        <td>@result.SecondaryVoltage</td>
                        <td>@result.Temperature</td>
                        <td>@result.Humidity</td>
                        <td>@result.TestDate</td>
                        <td>@result.TanFai</td>
                        <td>@result.R</td>
                    </tr>
                    <tr>
                        <td colspan="7">
                            <table class="table table-striped">
                                <thead>
                                    <tr>
                                        <th></th>
                                        <th>ao</th>
                                        <th>bo</th>
                                        <th>co</th>
                                    </tr>
                                </thead>
                                <tbody>
                                @for (int i = 0; i < 5; i++)
                                {
                                        <tr>
                                        @switch (i)
                                        {
                                            case 0:
                                                            <td>f(%)</td>
                                                break;
                                            case 1:
                                                            <td>d(分)</td>
                                                break;
                                            case 2:
                                                            <td>dU(%)</td>
                                                break;
                                            case 3:
                                                            <td>Upt:U</td>
                                                break;
                                            case 4:
                                                            <td>Uyb:U</td>
                                                break;
                                        }
                                        @for (int j = 0; j < 3; j++)
                                        {
                                                    <td>@result.TableDataArray[i*3+j]</td>
                                        }
                                        </tr>
                                }
                                </tbody>
                            </table>
                        </td>
                    </tr>
            }
            </tbody>
        </table>
}
else
{
        <p>No test results available.</p>
}
